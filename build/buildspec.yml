version: 0.2

env:
  shell: bash

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      # install dependencies
      - echo installing dependencies
      - npm ci

  pre_build:
    commands:
      # execute prebuild commands
      - echo pre build

  build:
    commands:
      - echo building
      # build app
      - ng build --prod

artifacts:
  files:
    - '**/*'
